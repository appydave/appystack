node_modules
dist
build
coverage
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode
.idea
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Scratch / working files
raw.txt
raw2.txt
raw-ralph.txt
/*.png

# Claude planning artifacts (ignore local settings, but track shareable rules)
.claude/*
!.claude/rules/
!.claude/rules/**

# Exception: template ships with skills for app recipe discovery
!template/.claude/
!template/.claude/**
!create-appystack/template/.claude/
!create-appystack/template/.claude/**
.playwright-mcp/

# Template build/test artifacts. The template ships its ignore file as `gitignore`
# (renamed for npm-pack), which stopped it ignoring these paths in THIS repo — so
# restore the intent at the root and stop run/test artifacts leaking as untracked.
template/package-lock.json
template/test-results/
create-appystack/template/package-lock.json
